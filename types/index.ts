export type GridCardProps = {
    title: string;
    image: string;
    description: string;
}

export type ProductCardProps = {
    title: string;
    description: string;
    noHover: () => void;
}